# XSDFormer

XSDFormer deforms XML schema definitions into protobuf definitions, and generates code for converting parsed XML into protobuf instances.

It supports enough of the XSD spec to convert ClinVar and BioC schemas; full support is a non-goal.
